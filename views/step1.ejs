<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Step 1 — Your Family</title>
    <link rel="stylesheet" href="/styles.css">
  </head>
  <body>
    <div class="container">
      <h2>Step 1: Your household</h2>
      <form method="post" action="/step1">
        <div>
          <label for="adults">Adults</label>
          <input id="adults" name="adults" type="number" min="0" value="<%= (userData.adults ?? '') %>" required>
        </div>
        <div>
          <label for="children">Children</label>
          <input id="children" name="children" type="number" min="0" value="<%= (userData.children ?? '') %>" required>
        </div>
        <div class="button-row">
          <button type="submit">Continue →</button>
          <a class="btn" href="/">Cancel</a>
        </div>
      </form>
    </div>
  </body>
</html>
